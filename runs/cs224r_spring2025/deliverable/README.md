# CS224R Spring 2025 Deliverable

This folder contains the final local handoff copy for the CS224R Spring 2025 textbook.

Files:

- `cs224r_complete_notes.pdf`
- `cs224r_complete_notes.tex`

The source build artifacts remain in `runs/cs224r_spring2025/build/`. The PDF is over GitHub's ordinary single-file size limit, so the duplicated handoff files are ignored by Git.
